## Packages
date-fns | Date formatting for schedules and appointments
framer-motion | Smooth animations for large UI elements
lucide-react | High contrast, clear icons for accessibility

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
